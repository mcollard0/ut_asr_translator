# WhatsApp Audio Speech-to-Text and Translation Requirements
# Core ML libraries
transformers>=4.44.0,<5.0.0
torch>=2.2.0
huggingface_hub>=0.24.0
accelerate>=0.33.0

# Language model dependencies
sentencepiece>=0.1.99
sacremoses>=0.0.53

# Language detection and processing
langdetect>=1.0.9     # Fallback text language detection

# Performance optimization (optional)
bitsandbytes>=0.41.0  # For 8-bit quantization (CUDA only)

# Audio processing
soundfile>=0.12.1
librosa>=0.10.1
ffmpeg-python>=0.2.0  # Audio format conversion

# UI and utilities
tqdm>=4.66.0
rich>=13.7.0
python-dotenv>=1.0.1

# Optional: for development and testing
pytest>=7.0.0
pytest-cov>=4.0.0